# Frontend Dockerfile
FROM node:18

WORKDIR /app

# 复制 package.json 和 package-lock.json
COPY frontend/chatbot/package*.json ./

# 安装所有依赖
RUN npm install

# 复制源代码
COPY frontend/chatbot/ ./

# 构建应用
RUN chmod +x node_modules/.bin/next && npm run build

# 暴露端口
EXPOSE 3000

# 启动应用
CMD ["npm", "start"]
